default:
  image: node:18-alpine

include:
  - project: 'internal/ci-scripts'
    ref: master
    file: '/web/ci-default.yml'

build:
  stage: build
  script:
    - npm install
    - npm run build:prod

deploy:
  extends: .deploy-to-dev-tpl
  variables:
    ENV: dev
  only:
    - main
